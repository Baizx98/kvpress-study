# Prefill Sweep 10pct: BlockWise vs SnapKV

## 目的

在 `10%` 数据集比例下，对 `BlockWisePress` 与 `SnapKV` 做 prefill-only 压缩对比。

## 相关结果

- 结果目录：
  `evaluation/results/experiments/prefill_sweep_10pct_blockwise_snapkv/artifacts/`
- 主图：
  `prefill_blockwise_vs_snapkv_10pct.png`

## 备注

这是较早的一轮 sweep，主要用于快速观察方法趋势，不是当前最新版默认参数的最终结论来源。
